import express, {Request, Response, NextFunction} from "express";
import db from "../config/db";

const addCreditCard = (req: Request, res: Response, next: NextFunction) =>{
    
};

const deleteCreditCard = (req: Request, res: Response, next: NextFunction) =>{
    
};

const updateCreditCard = (req: Request, res: Response, next: NextFunction) =>{
    
};

const getAllCreditCards = (req: Request, res: Response, next: NextFunction) =>{
    
};

export default {addCreditCard, deleteCreditCard, updateCreditCard, getAllCreditCards};